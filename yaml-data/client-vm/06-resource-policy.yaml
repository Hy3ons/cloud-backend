apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{VM_NAME}}_quota
  namespace: {{NAMESPACE}}

spec:
  hard:
    requests.cpu: "2"
    requests.memory: 3Gi
    persistentvolumeclaims: "5"
    count/virtualmachines.kubevirt.io: "2"